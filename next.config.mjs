/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Allow all local images; add external domains here when needed
    remotePatterns: [],
  },
};

export default nextConfig;
